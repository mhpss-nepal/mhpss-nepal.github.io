# Which pages the bilingual gate enforces.
#   enforced  = fully keyed and fully translated, or the build fails
#   pending   = not yet migrated; counted and reported, not blocked
#
# Move a page to "enforced" only once its keys are in and the Nepali has
# come back from the translator. Adding English prose to an enforced page
# without a key fails the gate -- which is the point.

# The public website (Layer 3), rebuilt to the mhpssmyanmar.org model on
# 16 Sep 2026: all eight pages are keyed from the day they were written, so no
# English prose can be added to them without a key. Nepali is still awaited
# for the long prose -- short strings carry machine drafts, marked as such.
# bps.html withdrawn 20 Sep 2026 on the Coordinator's instruction: the BPS+
# course is not part of this response, so the page and its keys were retired
# rather than left standing as an empty promise.
index.html keyed
flood-response.html keyed
iec.html keyed
referral-directory.html keyed
resources.html keyed
videos.html keyed
contact-us.html keyed
# The field forms moved to the sibling repository `form` on 17 Sep 2026 and
# are gated there.
